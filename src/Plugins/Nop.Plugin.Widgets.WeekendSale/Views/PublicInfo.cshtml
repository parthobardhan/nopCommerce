@model PublicInfoModel
<div class="weekend-sale-banner alert alert-success" role="status">
    <p>@Model.BannerText</p>
    @if (!string.IsNullOrEmpty(Model.OrderNumber))
    {
        <p>Order @Model.OrderNumber — next time use <strong>@Model.CouponCode</strong>.</p>
    }
</div>
